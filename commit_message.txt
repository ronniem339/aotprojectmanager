fix: Ensure onCameraDescriptions is correctly passed to AI function

- Modified `ShotListViewer.js` to directly pass `video.tasks.onCameraDescriptions` to `generateShotListFromScriptAI`.
- Removed the now-redundant `onCameraLocations` variable and its associated logging from `ShotListViewer.js`.