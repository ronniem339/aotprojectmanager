fix(scripting): Persist on-camera input mode and transcript data

- Ensured `onCameraInputMode` and `fullTranscript` are saved to `video.tasks` in the database.
- Updated `ScriptingWorkspaceModal` to initialize `onCameraInputMode` and `parsedTranscript` from `taskData` when the modal opens.
- This allows the scripting workspace to correctly resume from the `review_parsed_transcript` stage and retain the user's input mode and full transcript content across sessions.