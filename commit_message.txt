fix(scripting): Resolve ReferenceError for onCameraInputMode

- Passed `onCameraInputMode` explicitly to `handleGenerateFullScript` from `initiateScriptGeneration` and the `review_parsed_transcript` stage to resolve a `ReferenceError`.
- This ensures the `onCameraInputMode` is always accessible within the `handleGenerateFullScript` function, allowing correct logic for both per-location notes and full transcript input.